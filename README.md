# node-red-teknoir-ingest

A set of nodes that lets you ingest data from all your devices.

# Install

```
npm install --save node-red-teknoir-ingest
```

# Documentation
The nodes are properly documented in `Node-RED` itself. 
